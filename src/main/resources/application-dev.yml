spring:
  datasource:
    url: jdbc:mysql://localhost:3306/product
    username: root
    password: Welcome@1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

# to enable: SPRING_PROFILES_ACTIVE=dev